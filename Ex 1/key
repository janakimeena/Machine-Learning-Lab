1. List the datatypes of the fields read
DataFrame.info()

2. Find the number of null values in each field
DataFrame.info() - total - non-null

3. Analyze the distribution of value of variable 'loan_amount'
pd.crosstab(index=loan['loan_amount'], columns = count, dropna=True)

4. Find the unique values and their respective counts in the fields distribution_model and original_language


5. Identify the columns that may be dropped from dataset
country_code
activity
distribution_model

6. Check if the datatype of term_in_months is numeric datatype and convert to numeric if not
No
By identifying the string values present and replacing them with equivalent numerical values
dataframe['field'].replace('three',3,inplace=True)
dataframe['field'] = dataframe['field'].astype('int64')

7. Drop the missing values and find the percentage split of different categories of repayment_interval

8. What is the maximum loan amount disbursed in the Retail sector?
dataframe.describe()
9. Find out the frequency distribution of sector with respect to borrower_genders
pd.crosstab(index = loan['sector'],columns=loan['borrow_genders'],dropna=True)

10. Convert all categorical variables in a dataframen to indicator variable
 pandas.get_dummies(data) 
